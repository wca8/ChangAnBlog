import React, { memo } from "react";
import { AskWrapper } from "./style";

export default memo(function askQuestions() {
  return (
    <AskWrapper>
      <h2>askQuestions</h2>
    </AskWrapper>
  );
});
